{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: sat_predictor.py with iCal output",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/sat_predictor.py",
            "args": [
                "-output",
                "ical"
            ],
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
